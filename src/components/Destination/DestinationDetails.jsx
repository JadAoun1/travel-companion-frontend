// src/components/Destination/DestinationDetails.jsx

import { userParams } from 'react-router';
import { useEffect, useState } from 'react';

const DestinationDetails = () => {
    // const

    // useEffect

    // console.log()

    return (
        <main>
            <section>
                <header>
                    <h1>Destinations Dashboard</h1>
                    <p>See all destinations here</p>
                </header>
{/* show all attractions on this page. each should be linked to a new page with attraction details */}
            </section>
        </main>
    );
};

export default DestinationDetails;